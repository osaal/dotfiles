# My dotfiles

## TODOs and Current Issues

-   Adding the `dunst` theme seems to have slowed down the start of `waybar`. Issue needs to be investigated.
-   TODO: Icon packs should be added to make `dunst` icons work.
-   TODO: Colours should be matched with the main theme
-   The `rofi` script must be indicated in `dunstrc` and `hyprland.conf`.
